# ActiveHostScanner
ActiveHostScanner is a network scanning tool designed to quickly identify active hosts within a specified IP range.  It first attempts to detect hosts using ICMP (ping), and if unsuccessful, it performs a TCP SYN scan on commonly open ports.  This tool is useful for network administrators, penetration testers, and security professionals.
